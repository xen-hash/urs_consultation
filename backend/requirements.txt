flask
flask-socketio
flask-cors
# Signs the session tokens in security.py. Flask pulls it in transitively, but
# this app imports it directly, and an undeclared direct dependency breaks the
# moment the transitive one moves -- the same trap as `packaging` below.
itsdangerous
pg8000
# pymysql is only needed by migrate_mysql_to_postgres.py, the one-off script
# that copies old MySQL data across. Not used by the app itself. If you migrate
# from a MySQL 8 server using caching_sha2_password, also `pip install
# cryptography` — it is not needed otherwise, and not needed for Postgres.
pymysql
pytz
openpyxl
qrcode[pil]
Pillow
bcrypt
python-dotenv
requests
gevent
gevent-websocket
gunicorn
# gunicorn's gevent worker imports packaging.version, but gunicorn does not
# declare packaging as a dependency. Without this the worker class fails to
# load and the app won't boot on a clean install.
packaging
